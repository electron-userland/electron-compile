os:
  - linux
  - osx

sudo: false

language: node_js

node_js:
  - "6"

cache:
  directories:
    - node_modules

install:
  - npm i -g npm
  - npm install

script:
  - npm run compile
  - npm run test
